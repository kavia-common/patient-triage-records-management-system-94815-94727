# MongoDB connection configuration for database_triagens container
# The orchestrator will inject actual values into the .env file at runtime.
MONGODB_URL=mongodb://appuser:dbuser123@localhost:5000/?authSource=admin
MONGODB_DB=myapp

# Optional: default seed settings
SEED_BATCH_SIZE=50
